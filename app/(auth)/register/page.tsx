import { Metadata } from 'next';
import React from 'react'
import Register from './register';

const Page = () => {
  return (
    <><Register /></>
  )
}

export const metadata: Metadata =  {
    title: "Lend Connect | Register"
}

export default Page;